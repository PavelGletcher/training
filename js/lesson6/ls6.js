/*
//функции

function info(hello) {// функция (в скобки вводим параметры)
  console.log (hello + 'ку');
}

info("word"); // вызов функции


function summa(arr) { //функция с параметром
  var sum = 0; // переменная со значением 0
  for (var i = 0; i < arr.length; i++) // цикл фор от нуля до кол-ва элементов в массиве +1
  sum += arr[i]
  return sum; //возвращаем полученное личсло в любую нужную еперемнную для дальнейшей работы
}

var array = [5, 8, 1];
var array_1 = [2, 81, 1];
var array_2 = [45, 8, 1];
var res = summa (array)
console.log (res);
//summa (array_1)
//summa (array_2)
*/